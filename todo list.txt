custom theme.
histogram.
tools:
	ellipse (+ filled variant)
multi-window support.
scripting:
	float(boolean)
	user-defined methods
make animation not draw canvas when open image tab is not selected.
better compression for history/tool actions.
wrong layer draws to zoomable image when parent is showing but child is modified.
probably need to separate save file from export file.

blend modes:
	one input:
		negate
		clamp
		levels
	many inputs:
		alpha
		add
		add constant
		average
		multiply
		multiply constant
		inverse multiply
		min
		max

effects:
	gradient re-mapper.