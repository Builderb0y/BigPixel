custom theme.
tools:
	ellipse (+ filled variant)
multi-window support.
scripting:
	float(boolean)
	user-defined methods.
	loops.
make animation not draw canvas when open image tab is not selected.
better compression for history/tool actions.
probably need to separate save file from export file.
cullface.
tintIndex.
export model to minecraft-formatted json file.
use UI component builders for everything?
	maybe use manifold extension methods?
lwjgl for some complex layer sources?

cubic view doesn't update when dependencies change.
view splines don't update when moving the layer.

gradient remap "curve" parameter.
	applies to the mix level after converting to grayscale,
	and before mixing between 2 other colors.
	scripted.

layer sources:
	no inputs:
		blue noise pixels.
			basically generate a list of xy coordinates that follow a blue noise pattern.